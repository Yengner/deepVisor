// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  // inital to get things working first
  id       Int        @id @default(autoincrement())
  name     String? // User's name
  email    String     @unique // Email, should be unique for login
  password String // Hashed password for authentication
  fbId     String? // Facebook ID
  businessName String? // Business name
  // id           Int      @id @default(autoincrement())
  // name         String   @db.VarChar(100) // User's name
  // email        String   @unique @db.VarChar(255) // Email, should be unique for login
  // password     String   // Hashed password for authentication
  // phone        String?  @db.VarChar(15)  // Optional phone number
  // websiteUrl   String?  @db.VarChar(255) // Optional business website URL
  // address      String?  @db.VarChar(255) // Optional business address
  // createdAt    DateTime @default(now())  // Automatically sets the creation date
  // campaigns    Campaign[]                // One-to-many relationship with Campaigns
  // Campaign Campaign[]
}

// model Campaign {
//   id        Int      @id @default(autoincrement())
//   fbId      String   @unique // Facebook Campaign ID
//   name      String   @db.VarChar(255) // Campaign name
//   spend     Float // Amount spent
//   reach     Int // Number of people reached
//   clicks    Int // Number of clicks
//   createdAt DateTime @default(now()) // Campaign creation date
//   userId    Int // Foreign key to the user
//   user      User     @relation(fields: [userId], references: [id]) // User relation
  // lead      Lead[]   // One-to-many relationship with Leads
// }

// model Lead {
//   id           Int      @id @default(autoincrement())
//   name         String   @db.VarChar(100)  // Lead name
//   email        String   @db.VarChar(255)  // Lead email
//   phone        String?  @db.VarChar(15)   // Optional phone number
//   conversion   Boolean   @default(false)  // Whether the lead converted
//   campaignId   Int                         // Foreign key to the campaign
//   campaign     Campaign  @relation(fields: [campaignId], references: [id]) // Campaign relation
//   createdAt    DateTime   @default(now())  // Timestamp for when the lead was created
// }
